Shared telemetry code.

Note this is not separately compiled project. Tests live here, but telemetry.ts should simply be
symlinked into the respective project repository.

Take care to also add dependencies:

- applicationinsights
- conf
- read-pkg-up^7.0.1
