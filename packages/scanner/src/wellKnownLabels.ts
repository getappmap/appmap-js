enum WellKnownLabels {
  JobCreate = 'job.create', // a job created and enqueued for later execution
  JobCancel = 'job.cancel', // a job removed from execution queue
}

export default WellKnownLabels;
