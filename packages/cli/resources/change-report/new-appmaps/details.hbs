{{#if newAppMaps.length }}
<h2 id="new-appmaps">New AppMaps</h2>
  {{#each newAppMaps}}

[{{name}}]({{ appmap_url 'head' this }})

  {{/each}}
  {{#if pruned}}
Because there are so many new AppMaps, some of them are not listed in this report.
  {{/if}}
{{/if}}