name: appmap-js
appmap_dir: tmp/appmap
packages:
  - path: packages/cli
    exclude:
      - node_modules
      - .yarn
      - splitCamelized
      - isCamelized
  - path: packages/navie
    exclude:
      - node_modules
      - .yarn
  - path: packages/scanner
    exclude:
      - node_modules
      - .yarn
  - path: packages/sequence-diagram
    exclude:
      - node_modules
      - .yarn
